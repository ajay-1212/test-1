{
  "name": "webrtc-node",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "naught start --ipc-file logs/naught.ipc --log logs/naught.log --stdout logs/stdout.log --stderr logs/stderr.log app.js"
  },
  "dependencies": {
    "express": "*",
    "jade": "*",
    "socket.io": "*"
  }
}
